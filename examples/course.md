# TCP 连接管理

## 三次握手

TCP 三次握手用于同步双方初始序列号并确认双方收发能力。
客户端发送 SYN，服务端回复 SYN+ACK，客户端再回复 ACK。
仅两次握手时，服务端不能确认客户端是否收到自己的初始序列号。
答题先写三步报文，再说明序列号同步与确认关系。

## 四次挥手

TCP 是全双工连接，两个方向需要分别关闭。主动关闭方发送 FIN，
对方回复 ACK；对方发送完剩余数据后发送 FIN，主动关闭方回复 ACK。
ACK 与 FIN 有时可以合并，因此实际报文数量不一定恒为四。
